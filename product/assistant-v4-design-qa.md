# 助手模块 V4 视觉验收摘要

日期：2026-09-23

对齐问题的本轮验收记录：仓库根目录 `design-qa.md`（当前 `final result: passed`，仅覆盖输入框与底部导航的间距）。此文件记录助手 V4 更广范围的原型验收；尚未完成的页面覆盖仍为进行中。

## 已完成的静态检查

- 根项目及 `web/` TypeScript 类型检查通过。
- H5 本地构建通过，生成 3 个浏览器模块。
- Sites/Worker 构建通过；构建结果报告 `localDataIncluded: false`。
- `git diff --check` 通过。

以上仅证明源码可检查、构建，不代表页面已与原型逐屏一致。

## 逐屏对照

验收须在用户选择的 Chrome 或 Edge 中，以独立临时数据目录和窄屏视口打开本地 H5；对每页截图并与 `doc/screens/` 中对应原型比较。截图、实际尺寸、差异及修复记录补齐前，一律保持“待验收”。

| 原型 | 当前状态 | 浏览器截图 / 差异记录 |
| --- | --- | --- |
| A00 首页及建议、进行中、已完成状态 | 待验收 | — |
| A01 模式选择 | 待验收 | — |
| A02 问答会话 | 待验收 | — |
| A03 升级为任务 | 待验收 | — |
| A04 任务计划 | 待验收 | — |
| A05 执行中 | 待验收 | — |
| A06 等待输入 | 待验收 | — |
| A07 执行确认 | 待验收 | — |
| A09 结果 | 待验收 | — |
| A10 失败与恢复 | 待验收 | — |
| B01 任务列表 | 待验收 | — |
| B03 任务详情 | 待验收 | — |
| B04 执行依据 | 待验收 | — |
| B08 工具与权限 | 待验收 | — |
| C01/C02/C03 输入、语音及添加内容状态 | 待验收 | — |
| C04/C05 计划编辑与追加要求 | 待验收 | — |

## 验收边界

- 浏览器选择尚未收到；不得默认替用户选择 Chrome 或 Edge。
- Windows Computer Use 当前无法在此 Linux 工作区初始化。若后续仍不可用，应在具备可视浏览器的环境中完成截图，而不是把构建或源码推测当作视觉验收。
- 工作区 `work/h5-data/` 已有数据，验收不可使用或覆盖该目录；启动浏览器验证前应使用独立临时数据目录。
- 外部模型、语音转写、上传、PDF、日历、云盘与电脑同步能力未接入。这些画面只验证布局与诚实的不可用状态，不标记为对应集成功能通过。
